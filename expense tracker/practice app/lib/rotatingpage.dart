import 'package:flutter/material.dart';
class RotatingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rotating Page'),
      ),
      body: Center(
        child: Text('This is a rotating page.'),
      ),
    );
  }
}





